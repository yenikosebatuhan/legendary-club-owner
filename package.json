{
  "name": "legendary-club-owner-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Landing page for Legendary Club Owner — a mobile football club management simulation game.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --base=/legendary-club-owner/",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "predeploy": "npm run build && node -e \"require('fs').writeFileSync('dist/.nojekyll','')\"",
    "deploy": "gh-pages -d dist -t -b gh-pages"
  },
  "dependencies": {
    "lucide-react": "^0.453.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@vitejs/plugin-react": "^4.3.2",
    "autoprefixer": "^10.4.20",
    "gh-pages": "^6.3.0",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.9"
  }
}
